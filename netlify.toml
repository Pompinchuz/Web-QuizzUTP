[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
```

### 4. Crea un archivo `.env.example` para documentar

**`frontend/.env.example`**
```
VITE_API_URL=https://tu-backend-en-vercel.vercel.app/api
```

### 5. Actualiza el `.gitignore`

Asegúrate de que tu `.gitignore` incluya:
```
.env
.env.local
.env.production